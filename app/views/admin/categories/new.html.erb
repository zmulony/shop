<%= form_for [:admin, @category] do |t| %>

  <p>
    Name: <br/>
    <%= t.text_field :name %>
  </p>
  
  <p>
    <%= submit_tag "Create" %> <%= link_to "Back", admin_categories_path %>
  </p>
  
<% end %>